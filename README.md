# Autonomy.ai

This repository also contains the active **Agent Compute Gateway** exploration.

The current Gate 0 product area is in [`compute-gateway/`](compute-gateway/).

It documents the full journey from a Hugging Face `402 Payment Required` benchmark failure to a provider-neutral agent compute gateway, including the credential-plane decisions, competitive landscape, security model, architecture, and working Modal + Infisical prototype.

> The existing ML notebooks in this repository are preserved unchanged.
